<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="utf-8">
    <title>Local Fruits & Veg</title>
    <link rel="stylesheet" href="/stylesheets/main.css" />
  </head>

  <body>

    <p>Local Fruits & Veg</p>

    <div class="nav">
      <% if logged_in? %>
        <a href="/logout">Logout</a>
        <a href="/products">All available products</a>
        <a href="/products/new">New product</a>
      <% end %>

      <% if logged_in? %>
        <h4><%= "You are logged in as " %><a href="/users/<%= current_user.slug %>"><%= current_user.username %></a></h4>
      <% end %>

    </div>

    <div class="wrapper">

      <% if flash[:message] %>
        <h3 style="color: green;"><%= flash[:message] %></h3>
      <% end %>
      <% if flash[:errors] %>
        <h3 style="color: red;"><%= flash[:errors] %></h3>
      <% end %>

      <%= yield %>

      <footer class="branding">
        <small>&copy; 2019 <strong>Local Fruits & Veg</strong></small>
      </footer>

    </div>

    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1/jquery.min.js"></script>

  </body>
</html>
